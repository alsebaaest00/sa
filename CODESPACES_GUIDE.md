# 🚀 دليل البداية السريعة مع GitHub Codespaces

## ما هو GitHub Codespaces؟

GitHub Codespaces هو بيئة تطوير سحابية كاملة تعمل في متصفحك. لا تحتاج إلى تثبيت أي شيء على جهازك!

## 🎯 البدء في 3 خطوات

### 1️⃣ فتح Codespace

اضغط على هذا الزر:

[![Open in GitHub Codespaces](https://github.com/codespaces/badge.svg)](https://codespaces.new/alsebaaest00/sa)

أو من صفحة GitHub:
1. اضغط على زر **Code** (الأخضر)
2. اختر تبويب **Codespaces**
3. اضغط على **Create codespace on main**

### 2️⃣ انتظر إعداد البيئة (2-3 دقائق)

سيقوم Codespace تلقائياً بـ:
- ✅ تثبيت Python 3.11 + Poetry
- ✅ تثبيت جميع التبعيات
- ✅ تكوين VS Code مع الامتدادات
- ✅ إنشاء ملف `.env` من القالب
- ✅ تكوين Git hooks

شاهد التقدم في Terminal السفلي.

### 3️⃣ أضف مفاتيح API وابدأ!

```bash
# عدّل ملف .env
nano .env
```

أضف مفاتيحك:
```env
REPLICATE_API_TOKEN=r8_xxx...
OPENAI_API_KEY=sk-xxx...
ELEVENLABS_API_KEY=xxx...
```

ثم:
```bash
# تشغيل الواجهة
make run-ui

# أو تشغيل API
make run-api
```

## 🎨 استخدام البيئة

### الواجهة الرسومية (VS Code)

ستفتح VS Code تلقائياً مع:
- 🎨 Theme مريح للعين
- 🔧 جميع الامتدادات المطلوبة
- 📝 تنسيق تلقائي عند الحفظ
- 🐛 دعم debugging كامل

### Terminal المدمج

استخدم Terminal (Ctrl + `) لتشغيل الأوامر:

```bash
# عرض جميع الأوامر
make help

# تشغيل الاختبارات
make test

# فحص الكود
make lint

# تنسيق الكود
make format
```

### المنافذ (Ports)

عند تشغيل التطبيق، ستظهر المنافذ تلقائياً:
- **8501**: Streamlit UI - سيفتح في تبويب جديد
- **8000**: FastAPI - للـ API calls

افتح تبويب **PORTS** في الأسفل لرؤية الروابط.

## 💡 نصائح مهمة

### 🔐 حماية المفاتيح

بدلاً من `.env`، استخدم GitHub Secrets:

1. اذهب إلى Settings → Codespaces
2. أضف Secrets:
   - `REPLICATE_API_TOKEN`
   - `OPENAI_API_KEY`
   - `ELEVENLABS_API_KEY`
3. ستكون متاحة تلقائياً في كل Codespace!

### ⚡ تحسين الأداء

1. **اختر Machine المناسب:**
   - 2-core: للتطوير الأساسي
   - 4-core: للعمل مع الصور
   - 8-core: للعمل مع الفيديو (موصى به)

2. **أوقف Codespace عند عدم الاستخدام:**
   - يتوقف تلقائياً بعد 30 دقيقة خمول
   - أو أوقفه يدوياً من GitHub

3. **استخدم Cache:**
   ```python
   # في الكود، استخدم cache للطلبات المكررة
   generator.generate(prompt, use_cache=True)
   ```

### 🔄 إعادة بناء الـ Container

إذا واجهت مشاكل:

1. افتح Command Palette (F1)
2. ابحث عن: `Codespaces: Rebuild Container`
3. اختر **Rebuild**

## 🚀 سير العمل الموصى به

### للمبتدئين

```bash
# 1. تحقق من البيئة
python --version  # يجب أن يكون 3.11+
poetry --version

# 2. جرب الأمثلة
python examples.py

# 3. شغل الواجهة
make run-ui
```

### للمطورين

```bash
# 1. إنشاء فرع جديد
git checkout -b feature/my-feature

# 2. اكتب الكود
# ...

# 3. اختبر تغييراتك
make test
make lint

# 4. Commit & Push
git add .
git commit -m "feat: add my feature"
git push origin feature/my-feature
```

## 🆚 Codespaces vs. التثبيت المحلي

| الميزة | Codespaces ☁️ | محلي 💻 |
|--------|--------------|---------|
| وقت الإعداد | 2-3 دقائق | 10-15 دقيقة |
| المتطلبات | متصفح فقط | Python, Poetry, FFmpeg, إلخ |
| الأداء | يعتمد على machine type | يعتمد على جهازك |
| التكلفة | مجاني 60 ساعة/شهر | مجاني |
| الوصول | من أي جهاز | جهازك فقط |
| التخزين | 32-64 GB | غير محدود |

## 📚 موارد إضافية

- 📖 [توثيق Codespaces الرسمي](https://docs.github.com/en/codespaces)
- 🎓 [دليل Dev Containers](https://containers.dev/)
- 🔧 [تخصيص Codespace](.devcontainer/README.md)
- 💬 [دعم GitHub Community](https://github.community)

## 🐛 حل المشاكل الشائعة

### المشكلة: "Port 8501 already in use"

```bash
# أوقف العملية القديمة
pkill -f streamlit

# أو استخدم port مختلف
poetry run streamlit run src/sa/ui/app.py --server.port 8502
```

### المشكلة: "Module not found"

```bash
# أعد تثبيت التبعيات
poetry install --no-interaction
```

### المشكلة: "API Key not found"

تأكد من:
1. وجود ملف `.env` في المجلد الرئيسي
2. المفاتيح بالصيغة الصحيحة
3. لا توجد مسافات إضافية

```bash
# تحقق من المحتوى
cat .env
```

### المشكلة: الأداء بطيء

1. تحقق من machine type الحالي
2. أغلق البرامج والتبويبات غير المستخدمة
3. استخدم machine أكبر إذا لزم الأمر

## ✅ أنت جاهز!

الآن يمكنك:
- ✨ توليد صور من النصوص
- 🎬 إنشاء فيديوهات
- 🎤 تحويل النص إلى صوت
- 🎨 استكشاف القوالب الجاهزة

**استمتع بالتطوير!** 🚀

---

**💬 هل تحتاج مساعدة؟** [افتح Issue](https://github.com/alsebaaest00/sa/issues/new)
