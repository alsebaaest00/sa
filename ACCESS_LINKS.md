# 🌐 روابط الدخول

## 📍 كيفية الوصول في GitHub Codespaces

### الطريقة الأسهل ✅

1. **انظر أسفل VS Code** (في شريط الأدوات السفلي)
2. اضغط على تبويب **"PORTS"** (بجانب Terminal)
3. ستظهر قائمة المنافذ المفتوحة

### 🎨 الواجهة الرئيسية (Streamlit)
- **المنفذ**: 8501
- **الإجراء**:
  - ابحث عن السطر الذي يحتوي على `8501`
  - انظر لعمود "Forwarded Address"
  - اضغط على أيقونة 🌐 (Globe) لفتح في المتصفح الجديد
  - أو اضغط على الرابط مباشرة لنسخه

### 🔌 الـ API (FastAPI)
- **المنفذ**: 8000
- **الإجراء**:
  - ابحث عن السطر الذي يحتوي على `8000`
  - انظر لعمود "Forwarded Address"
  - اضغط على أيقونة 🌐 (Globe)

---

## 📚 روابط API المهمة

بعد فتح رابط API (المنفذ 8000)، أضف هذه المسارات:

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| التوثيق التفاعلي | `/docs` | Swagger UI |
| التوثيق البديل | `/redoc` | ReDoc |
| فحص الصحة | `/api/health` | Health Check |
| قائمة المشاريع | `/api/projects` | Projects API |
| الإحصائيات | `/api/statistics` | Statistics |

---

## 🖼️ مثال على الرابط

الرابط سيكون بهذا الشكل:
```
https://[codespace-name]-8501.app.github.dev
https://[codespace-name]-8000.app.github.dev
```

---

## 🔧 إذا لم تظهر المنافذ

شغل هذا الأمر:
```bash
cd /workspaces/sa
pkill -f "streamlit|uvicorn"
nohup poetry run streamlit run src/sa/ui/app.py --server.port 8501 --server.address 0.0.0.0 &
nohup poetry run uvicorn sa.api:app --host 0.0.0.0 --port 8000 &
```

---

## ✅ التحقق من التشغيل

```bash
# تحقق من الخوادم
ps aux | grep -E "(streamlit|uvicorn)" | grep -v grep

# تحقق من السجلات
tail -f /tmp/streamlit.log
tail -f /tmp/fastapi.log
```

---

**🎉 استمتع بالاستخدام!**
