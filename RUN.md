# 🚀 دليل التشغيل السريع

## 📋 جدول المحتويات
- [تشغيل الواجهة](#تشغيل-الواجهة)
- [تشغيل الـ API](#تشغيل-الـ-api)
- [تشغيل الاختبارات](#تشغيل-الاختبارات)
- [الأوامر المفيدة](#الأوامر-المفيدة)

---

## 🎨 تشغيل الواجهة

### Streamlit UI
```bash
poetry run streamlit run src/sa/ui/app.py
```

**الرابط:**
- 🌐 المحلي: `http://localhost:8501`
- 🌐 الشبكة: `http://0.0.0.0:8501`

**الميزات المتاحة:**
- 🖼️ توليد الصور
- 🎬 توليد الفيديو
- 🎤 توليد الصوت
- 🎯 مشروع كامل
- 📂 المعرض
- 📚 القوالب والنصائح

---

## 🔌 تشغيل الـ API

### FastAPI Server
```bash
poetry run python -m uvicorn sa.api:app --host 0.0.0.0 --port 8000 --reload
```

**الروابط:**
- 🌐 الصفحة الرئيسية: `http://localhost:8000`
- 📚 التوثيق التفاعلي (Swagger): `http://localhost:8000/docs`
- 📖 التوثيق البديل (ReDoc): `http://localhost:8000/redoc`
- ❤️ فحص الصحة: `http://localhost:8000/api/health`

**Endpoints المتاحة:**
```
GET    /                        - الصفحة الرئيسية
GET    /api/health             - فحص الصحة
GET    /api/projects           - قائمة المشاريع
GET    /api/projects/{id}      - تفاصيل مشروع
POST   /api/projects           - إنشاء مشروع
PUT    /api/projects/{id}      - تحديث مشروع
DELETE /api/projects/{id}      - حذف مشروع
POST   /api/generate/image     - توليد صورة
POST   /api/generate/audio     - توليد صوت
GET    /api/statistics         - الإحصائيات
```

### اختبار الـ API بـ curl
```bash
# فحص الصحة
curl http://localhost:8000/api/health

# قائمة المشاريع
curl http://localhost:8000/api/projects

# إنشاء مشروع جديد
curl -X POST http://localhost:8000/api/projects \
  -H "Content-Type: application/json" \
  -d '{"name":"مشروع تجريبي","description":"وصف المشروع"}'
```

---

## 🧪 تشغيل الاختبارات

### جميع الاختبارات
```bash
poetry run pytest -v
```

### اختبارات محددة
```bash
# اختبارات القوالب فقط
poetry run pytest tests/test_templates.py -v

# اختبارات قاعدة البيانات فقط
poetry run pytest tests/test_database.py -v

# اختبارات الصور فقط
poetry run pytest tests/test_image_generator.py -v
```

### مع تقرير التغطية
```bash
poetry run pytest --cov=sa --cov-report=html
```

### اختبار سريع (بدون تفاصيل)
```bash
poetry run pytest -q
```

---

## ⚡ الأوامر المفيدة

### تثبيت التبعيات
```bash
poetry install --no-interaction
```

### تحديث التبعيات
```bash
poetry update
```

### تنسيق الكود
```bash
poetry run black .
```

### فحص الكود
```bash
poetry run ruff check .
```

### تشغيل كل شيء معاً

#### في نوافذ منفصلة:
```bash
# نافذة 1: الواجهة
poetry run streamlit run src/sa/ui/app.py

# نافذة 2: الـ API
poetry run python -m uvicorn sa.api:app --host 0.0.0.0 --port 8000 --reload

# نافذة 3: الاختبارات (مراقبة التغييرات)
poetry run pytest-watch
```

---

## 🔧 إعداد البيئة

### إنشاء ملف `.env`
```bash
cp .env.example .env
```

### تعديل المفاتيح
```env
REPLICATE_API_KEY=your_replicate_key
OPENAI_API_KEY=your_openai_key
ELEVENLABS_API_KEY=your_elevenlabs_key
```

---

## 📊 المراقبة

### عرض السجلات
```bash
# سجلات Streamlit
tail -f ~/.streamlit/logs/*.log

# سجلات FastAPI (مع uvicorn)
# تظهر تلقائياً في الطرفية
```

### فحص قاعدة البيانات
```bash
# فتح قاعدة البيانات
sqlite3 data/sa.db

# عرض الجداول
.tables

# عرض المشاريع
SELECT * FROM projects;

# الخروج
.quit
```

---

## 🐛 استكشاف الأخطاء

### الواجهة لا تعمل
```bash
# تحقق من المنفذ
lsof -i :8501

# تشغيل على منفذ مختلف
poetry run streamlit run src/sa/ui/app.py --server.port 8502
```

### الـ API لا يعمل
```bash
# تحقق من المنفذ
lsof -i :8000

# تشغيل على منفذ مختلف
poetry run uvicorn sa.api:app --port 8001
```

### مشاكل التبعيات
```bash
# إعادة تثبيت
poetry install --no-cache

# حذف البيئة وإعادة إنشائها
poetry env remove python
poetry install
```

---

## 🌐 الوصول من الشبكة

### معرفة عنوان IP الخاص بك
```bash
# Linux/Mac
hostname -I

# أو
ifconfig | grep "inet "
```

### الوصول من جهاز آخر
```
الواجهة: http://YOUR_IP:8501
API: http://YOUR_IP:8000
```

---

## 📦 إنشاء نسخة قابلة للتوزيع

### Build Package
```bash
poetry build
```

### تشغيل من الحزمة
```bash
pip install dist/sa-*.whl
```

---

## 🔒 الأمان

**⚠️ ملاحظة مهمة:**
- لا تشارك مفاتيح API
- استخدم `.env` للمفاتيح السرية
- لا ترفع `.env` إلى git

---

## 📝 ملاحظات

- **الواجهة (Streamlit)**: تعمل على المنفذ `8501`
- **API (FastAPI)**: تعمل على المنفذ `8000`
- **قاعدة البيانات**: `data/sa.db` (يتم إنشاؤها تلقائياً)

---

**🎉 استمتع بالاستخدام!**
