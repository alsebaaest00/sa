# 🧪 اختبار تكوين GitHub Codespaces

هذا المستند يوضح كيفية اختبار تكوين Codespaces للتأكد من أنه يعمل بشكل صحيح.

## ✅ قائمة التحقق

عند فتح Codespace جديد، تحقق من:

### 1. البيئة الأساسية
- [ ] Python 3.11+ مثبت (`python --version`)
- [ ] Poetry مثبت (`poetry --version`)
- [ ] Git مثبت (`git --version`)
- [ ] FFmpeg مثبت (`ffmpeg -version`)

### 2. التبعيات
- [ ] تم تثبيت تبعيات Python (`poetry show`)
- [ ] ملف `.env` موجود
- [ ] المجلدات الأساسية موجودة (outputs/, logs/, data/)

### 3. امتدادات VS Code
- [ ] Python extension
- [ ] Pylance
- [ ] Black formatter
- [ ] Ruff
- [ ] GitHub Copilot (إن وُجد)
- [ ] GitLens

### 4. الإعدادات
- [ ] تنسيق تلقائي عند الحفظ
- [ ] حد السطر 100 حرف
- [ ] pytest مُفعّل

### 5. الأوامر
- [ ] `make help` يعمل
- [ ] `make lint` يعمل
- [ ] `make test` يعمل (قد يفشل بعض الاختبارات بدون API keys)
- [ ] `make format` يعمل

### 6. المنافذ
- [ ] Port 8501 (Streamlit) يظهر في تبويب Ports
- [ ] Port 8000 (FastAPI) يظهر في تبويب Ports

## 🔧 اختبارات يدوية

### اختبار 1: تشغيل الاختبارات
```bash
poetry run pytest -v
```
**النتيجة المتوقعة:** تشغيل الاختبارات (قد تفشل بعضها بدون API keys)

### اختبار 2: فحص الكود
```bash
make lint
```
**النتيجة المتوقعة:** فحص الكود بدون أخطاء كبيرة

### اختبار 3: تنسيق الكود
```bash
make format
```
**النتيجة المتوقعة:** تنسيق الملفات بنجاح

### اختبار 4: استيراد المكتبات
```bash
python -c "from sa.generators import ImageGenerator, VideoGenerator, AudioGenerator; print('✅ Imports successful')"
```
**النتيجة المتوقعة:** طباعة "✅ Imports successful"

### اختبار 5: تشغيل Streamlit (اختياري)
```bash
poetry run streamlit run src/sa/ui/app.py
```
**النتيجة المتوقعة:** 
- تشغيل الخادم على port 8501
- فتح الرابط تلقائياً
- عرض واجهة SA

### اختبار 6: تشغيل API (اختياري)
```bash
poetry run uvicorn src.sa.api.routes:app --host 0.0.0.0 --port 8000
```
**النتيجة المتوقعة:**
- تشغيل الخادم على port 8000
- الوصول إلى `/docs` للـ API documentation

## 🐛 استكشاف الأخطاء

### المشكلة: التبعيات لم تُثبّت
**الحل:**
```bash
cd /workspace
poetry install --no-interaction
```

### المشكلة: الامتدادات غير مثبتة
**الحل:**
1. افتح Command Palette (F1)
2. `Developer: Reload Window`
3. أو أعد بناء Container: `Codespaces: Rebuild Container`

### المشكلة: ملف .env غير موجود
**الحل:**
```bash
cp .env.example .env
```

### المشكلة: المنافذ لا تفتح تلقائياً
**الحل:**
1. افتح تبويب "PORTS" في الأسفل
2. اضغط "Forward a Port"
3. أدخل 8501 أو 8000

## 📊 تقرير الاختبار

بعد إكمال الاختبارات، سجّل النتائج:

| الاختبار | الحالة | ملاحظات |
|---------|--------|----------|
| Python 3.11+ | ⬜ | |
| Poetry | ⬜ | |
| التبعيات | ⬜ | |
| الامتدادات | ⬜ | |
| الأوامر | ⬜ | |
| المنافذ | ⬜ | |
| Imports | ⬜ | |

**الحالة:** ✅ نجح | ⚠️ تحذير | ❌ فشل

## 🎯 الخطوات التالية

إذا نجحت جميع الاختبارات:
1. ✅ التكوين جاهز للاستخدام
2. 🚀 يمكن البدء في التطوير
3. 📝 حدّث التوثيق إذا لزم الأمر

إذا فشلت بعض الاختبارات:
1. 📋 سجّل الأخطاء
2. 🔍 راجع logs
3. 🐛 افتح Issue مع التفاصيل
